[build]
  # Specifies the root directory of your site for publishing.
  publish = "."

[functions]
  directory = "netlify/functions/"
  # Increase the timeout for all functions to 26 seconds
  timeout = 26